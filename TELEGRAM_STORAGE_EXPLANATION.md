# 💾 Где хранятся подписчики Telegram?

## ПРОБЛЕМА текущего решения:
```javascript
const subscribers = new Map(); // ❌ Это В ПАМЯТИ функции!
```

**Что происходит:**
- Функция стартует → `subscribers = new Map()`
- Пользователь пишет `/start` → данные сохраняются в Map
- Функция перезагружается или истекает → **ВСЕ ДАННЫЕ ТЕРЯЮТСЯ!**
- В следующий раз пользователь `/start` → ВСЕ ПОДПИСЧИКИ ПОТЕРЯНЫ!

## РЕШЕНИЕ: Сохранять в YDB (Yandex Database)

### Таблица для подписчиков:
```sql
CREATE TABLE telegram_subscribers (
  id SERIAL PRIMARY KEY,
  chat_id BIGINT UNIQUE NOT NULL,
  username VARCHAR,
  first_name VARCHAR,
  subscribed_at TIMESTAMP DEFAULT NOW(),
  is_active BOOLEAN DEFAULT true
);
```

### Как работает правильно:
```
Пользователь → /start 
    ↓
Функция сохраняет в YDB
    ↓
ПОСТОЯННАЯ база данных!
    ↓
Функция перезагружается
    ↓
Данные ВСЕ ЕЩЕ в БД! ✅
```

## СЕЙЧАС:
- Если функция перезагружается → подписчики теряются
- Рассылка будет отправляться только активным подписчикам в памяти

## НУЖНО:
Подключить функцию к YDB и сохранять туда подписчиков
