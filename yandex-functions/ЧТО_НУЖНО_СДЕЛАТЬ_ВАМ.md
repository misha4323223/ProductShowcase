# 📋 ЧТО НУЖНО СДЕЛАТЬ ВАМ - Пошаговая инструкция

## 🎯 Я уже сделал за вас:

✅ Создал библиотеку для работы с API Яндекс.Доставки  
✅ Создал 4 Cloud Functions (расчет, создание, отслеживание, отмена)  
✅ Подготовил примеры React компонентов для фронтенда  
✅ Написал подробную документацию  

---

## 📝 ЧТО ОСТАЛОСЬ СДЕЛАТЬ ВАМ:

---

### ШАГ 1: Деплой Cloud Functions в Yandex Cloud

Вам нужно загрузить 4 функции в Yandex Cloud. Делайте по одной функции за раз.

#### 1.1 Откройте Yandex Cloud Console

1. Перейдите на: https://console.cloud.yandex.ru
2. Войдите в свой аккаунт
3. Выберите ваш каталог (folder)
4. В левом меню выберите **"Cloud Functions"**

---

#### 1.2 Создайте первую функцию: `calculate-delivery-yandex`

**Шаг 1:** Нажмите кнопку **"Создать функцию"**

**Шаг 2:** Заполните поля:
- **Имя**: `calculate-delivery-yandex`
- **Описание**: Расчет стоимости доставки Яндекс.Доставка

**Шаг 3:** Нажмите **"Создать"**

**Шаг 4:** В редакторе кода:
1. Выберите **Runtime**: `nodejs18`
2. Выберите **"Редактор кода"**
3. Создайте файл `index.js`:
   - Скопируйте содержимое файла `yandex-functions/calculate-delivery-yandex/index.js`
   - Вставьте в редактор
4. Создайте папку `lib`:
   - Нажмите **"Создать файл"**
   - Введите имя: `lib/yandex-delivery-client.js`
   - Скопируйте содержимое файла `yandex-functions/lib/yandex-delivery-client.js`
   - Вставьте в редактор

**Шаг 5:** Настройте параметры:
- **Точка входа**: `index.handler`
- **Timeout**: 30 секунд
- **Memory**: 128 MB

**Шаг 6:** Добавьте переменные окружения (пока тестовые):
```
YANDEX_DELIVERY_TOKEN=y2_AgAAAAD04omrAAAPeAAAAAACRpC94Qk6Z5rUTgOcTgYFECJllXYKFx8
YANDEX_PLATFORM_STATION_ID=fbed3aa1-2cc6-4370-ab4d-59c5cc9bb924
YANDEX_DELIVERY_TEST_MODE=true
```

**Шаг 7:** Нажмите **"Создать версию"**

**Шаг 8:** Сделайте функцию публичной:
1. Перейдите во вкладку **"Обзор"**
2. Нажмите **"Сделать публичной"**
3. **ВАЖНО**: Скопируйте **"URL вызова"** - он понадобится для API Gateway!
   - Например: `https://functions.yandexcloud.net/d4e...abc`

---

#### 1.3 Создайте вторую функцию: `create-yandex-order`

Повторите все шаги из 1.2, но:
- **Имя**: `create-yandex-order`
- **Код**: используйте файл `yandex-functions/create-yandex-order/index.js`
- Не забудьте также создать папку `lib` с файлом `yandex-delivery-client.js`
- Добавьте те же переменные окружения
- Скопируйте **URL вызова**

---

#### 1.4 Создайте третью функцию: `track-yandex-order`

Повторите все шаги из 1.2, но:
- **Имя**: `track-yandex-order`
- **Код**: используйте файл `yandex-functions/track-yandex-order/index.js`
- Не забудьте также создать папку `lib` с файлом `yandex-delivery-client.js`
- Добавьте те же переменные окружения
- Скопируйте **URL вызова**

---

#### 1.5 Создайте четвертую функцию: `cancel-yandex-order`

Повторите все шаги из 1.2, но:
- **Имя**: `cancel-yandex-order`
- **Код**: используйте файл `yandex-functions/cancel-yandex-order/index.js`
- Не забудьте также создать папку `lib` с файлом `yandex-delivery-client.js`
- Добавьте те же переменные окружения
- Скопируйте **URL вызова**

---

### ШАГ 2: Обновите API Gateway

#### 2.1 Откройте ваш API Gateway spec

1. Откройте файл `api-gateway-spec.yaml` (или как он у вас называется)

#### 2.2 Добавьте новые endpoints

Найдите секцию `paths:` и добавьте следующий код:

```yaml
  /api/delivery/yandex/calculate:
    post:
      summary: Расчет стоимости доставки Яндекс.Доставка
      operationId: calculateYandexDelivery
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ВСТАВЬТЕ_ID_calculate-delivery-yandex>
        service_account_id: <ваш_service_account_id>
      responses:
        '200':
          description: Успешный расчет

  /api/delivery/yandex/create:
    post:
      summary: Создание заявки
      operationId: createYandexOrder
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ВСТАВЬТЕ_ID_create-yandex-order>
        service_account_id: <ваш_service_account_id>
      responses:
        '200':
          description: Заявка создана

  /api/delivery/yandex/track:
    get:
      summary: Отслеживание заявки
      operationId: trackYandexOrder
      parameters:
        - name: claim_id
          in: query
          required: true
          schema:
            type: string
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ВСТАВЬТЕ_ID_track-yandex-order>
        service_account_id: <ваш_service_account_id>
      responses:
        '200':
          description: Информация о заявке

  /api/delivery/yandex/cancel:
    post:
      summary: Отмена заявки
      operationId: cancelYandexOrder
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ВСТАВЬТЕ_ID_cancel-yandex-order>
        service_account_id: <ваш_service_account_id>
      responses:
        '200':
          description: Заявка отменена
```

#### 2.3 Как получить function_id:

1. Откройте Cloud Function в консоли
2. Скопируйте **ID функции** (находится вверху страницы)
3. Вставьте вместо `<ВСТАВЬТЕ_ID_...>`

#### 2.4 Обновите API Gateway:

**Через веб-интерфейс:**
1. Откройте **API Gateway** в Yandex Cloud Console
2. Выберите ваш API Gateway
3. Нажмите **"Изменить"**
4. Вставьте обновленный spec
5. Нажмите **"Сохранить"**

**Или через CLI:**
```bash
yc serverless api-gateway update <ваш_api_gateway_id> \
  --spec=api-gateway-spec.yaml
```

---

### ШАГ 3: Тестирование

#### 3.1 Протестируйте расчет стоимости

Откройте терминал и выполните:

```bash
curl -X POST https://ваш-api-gateway.apigw.yandexcloud.net/api/delivery/yandex/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {
        "quantity": 1,
        "size": {"length": 0.5, "width": 0.5, "height": 0.5},
        "weight": 1
      }
    ],
    "route_points": [
      {
        "coordinates": [37.6156, 55.7558],
        "type": "source"
      },
      {
        "coordinates": [37.6492, 55.7558],
        "type": "destination"
      }
    ]
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "data": {
    "price": "...",
    ...
  }
}
```

---

### ШАГ 4: Получение боевого токена (когда договор будет готов)

#### 4.1 Заключите договор с Яндекс.Доставкой

1. Перейдите на https://dostavka.yandex.ru
2. Нажмите **"Стать клиентом"**
3. Выберите **"Самозанятый"** или **"ИП"** (в зависимости от вашего статуса)
4. Заполните анкету
5. Дождитесь звонка менеджера

#### 4.2 Получите токен

После заключения договора:
1. Войдите в личный кабинет: https://dostavka.yandex.ru
2. Перейдите в раздел **"Интеграция"**
3. Нажмите **"Активировать интеграцию"**
4. Нажмите **"Получить токен"**
5. Скопируйте токен

#### 4.3 Обновите переменные окружения

Для **КАЖДОЙ** из 4 функций:
1. Откройте функцию в консоли
2. Перейдите в **"Редактор"**
3. Измените переменные окружения:
```
YANDEX_DELIVERY_TOKEN=ваш_новый_боевой_токен_здесь
YANDEX_DELIVERY_TEST_MODE=false
```
4. Нажмите **"Создать версию"**

---

### ШАГ 5: Переход с самозанятого на ИП (когда станете ИП)

Когда вы станете ИП:

1. ✅ Получите новый токен в личном кабинете Яндекс.Доставки
2. ✅ Обновите `YANDEX_DELIVERY_TOKEN` во всех 4 функциях
3. ✅ **Всё!** Код менять не нужно

---

## 📊 Чеклист выполнения:

- [ ] Создал функцию `calculate-delivery-yandex`
- [ ] Создал функцию `create-yandex-order`
- [ ] Создал функцию `track-yandex-order`
- [ ] Создал функцию `cancel-yandex-order`
- [ ] Добавил переменные окружения во все функции
- [ ] Сделал все функции публичными
- [ ] Скопировал URL вызова для каждой функции
- [ ] Обновил API Gateway spec
- [ ] Протестировал расчет стоимости
- [ ] (После договора) Получил боевой токен
- [ ] (После договора) Обновил переменные окружения

---

## 🆘 Если что-то не работает:

1. **Ошибка "token not configured"**:
   - Проверьте, что добавили переменные окружения
   - Проверьте, что создали новую версию функции после добавления переменных

2. **Ошибка 404 при вызове API**:
   - Проверьте, что правильно обновили API Gateway
   - Проверьте, что указали правильные function_id

3. **Ошибка при расчете доставки**:
   - Проверьте формат запроса (координаты в формате [долгота, широта])
   - Проверьте, что используете тестовый токен для тестового сервера

---

## ✅ Готово!

После выполнения всех шагов у вас будет:
- ✅ Рабочая интеграция с Яндекс.Доставкой (на тестовом токене)
- ✅ 4 Cloud Functions готовы к использованию
- ✅ API Gateway настроен
- ✅ Код готов к переходу на боевой токен

**Сообщите мне когда закончите каждый шаг, и я помогу, если возникнут проблемы!** 🚀
