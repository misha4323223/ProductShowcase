# 📦 Руководство по настройке интеграции СДЭК

## 🎯 Важно!
Эта интеграция работает **одинаково** для самозанятых и ИП. 
При переходе с самозанятого на ИП нужно только **поменять API ключи** - код менять не нужно!

---

## 📝 Шаг 1: Получение API ключей СДЭК

### 1.1 Заключите договор с СДЭК

**Для самозанятых:**
1. Перейдите на сайт: https://cdek.business/
2. Нажмите "Заключить договор онлайн"
3. Выберите статус "Самозанятый"
4. Подготовьте справку из приложения "Мой налог"
5. Заполните анкету и дождитесь подтверждения

**Для ИП:**
1. Перейдите на сайт: https://cdek.business/
2. Нажмите "Заключить договор онлайн"
3. Выберите статус "ИП"
4. Укажите реквизиты вашего ИП
5. Заполните анкету и дождитесь подтверждения

### 1.2 Получите API ключи

1. После заключения договора войдите в **Личный кабинет СДЭК**: https://lk.cdek.ru
2. Перейдите в раздел **"Интеграция"**
3. Нажмите **"Создать новый ключ"**
4. Сохраните полученные данные:
   - **Account** (это будет ваш `CDEK_CLIENT_ID`)
   - **Secure password** (это будет ваш `CDEK_CLIENT_SECRET`)

⚠️ **Важно:** Сохраните эти ключи в безопасном месте! Они нужны для следующего шага.

---

## 🔧 Шаг 2: Настройка переменных окружения в Yandex Cloud

### 2.1 Создайте файл с переменными окружения

Для каждой Cloud Function нужно добавить переменные окружения:

1. Перейдите в **Yandex Cloud Console**: https://console.cloud.yandex.ru
2. Выберите ваш каталог (folder)
3. Перейдите в раздел **"Cloud Functions"**
4. Для **каждой** из следующих функций:
   - `calculate-delivery-cdek`
   - `get-pvz-cdek`
   - `create-cdek-order`
   - `track-cdek-order`

5. Добавьте переменные окружения:

```bash
CDEK_CLIENT_ID=ваш_account_из_личного_кабинета
CDEK_CLIENT_SECRET=ваш_secure_password_из_личного_кабинета
CDEK_TEST_MODE=false
```

⚠️ **Для тестирования** можно использовать тестовые ключи:
```bash
CDEK_CLIENT_ID=EMscd6r9JnFiQ3bLoyjJY6eM78JrJceI
CDEK_CLIENT_SECRET=PjLZkKBHEiLK3YsjtNrt3TGNG0ahs3kG
CDEK_TEST_MODE=true
```

### 2.2 Как добавить переменные в Cloud Function

В интерфейсе каждой функции:
1. Откройте вкладку **"Редактор"**
2. Прокрутите вниз до раздела **"Параметры"**
3. Найдите **"Переменные окружения"**
4. Добавьте каждую переменную:
   - Нажмите **"Добавить переменную"**
   - Введите имя (например, `CDEK_CLIENT_ID`)
   - Введите значение
5. Нажмите **"Создать версию"**

---

## 📤 Шаг 3: Загрузка функций в Yandex Cloud

### 3.1 Подготовка файлов

Для каждой функции нужно загрузить:
- `index.js` - основной файл функции
- `package.json` - зависимости
- **Общая библиотека** `lib/cdek-client.js` - должна быть в каждой функции

### 3.2 Создание zip-архивов

Создайте zip-архив для каждой функции, включая общую библиотеку:

**Для `calculate-delivery-cdek`:**
```bash
cd yandex-functions
zip -r calculate-delivery-cdek.zip calculate-delivery-cdek/ lib/
```

**Для `get-pvz-cdek`:**
```bash
zip -r get-pvz-cdek.zip get-pvz-cdek/ lib/
```

**Для `create-cdek-order`:**
```bash
zip -r create-cdek-order.zip create-cdek-order/ lib/
```

**Для `track-cdek-order`:**
```bash
zip -r track-cdek-order.zip track-cdek-order/ lib/
```

### 3.3 Загрузка через Yandex Cloud Console

Для каждой функции:

1. Перейдите в **Cloud Functions** → **"Создать функцию"**
2. Введите имя функции (например, `calculate-delivery-cdek`)
3. Нажмите **"Создать"**
4. В редакторе выберите **"ZIP-архив"**
5. Загрузите созданный zip-файл
6. Укажите **точку входа**: `index.handler`
7. Выберите **среду выполнения**: `nodejs18` или `nodejs20`
8. Добавьте **переменные окружения** (см. Шаг 2.2)
9. Нажмите **"Создать версию"**

---

## 🌐 Шаг 4: Настройка API Gateway

### 4.1 Обновите спецификацию API Gateway

Добавьте маршруты для СДЭК в ваш файл `api-gateway-spec.yaml`:

```yaml
paths:
  /api/delivery/cdek/calculate:
    post:
      summary: Рассчитать стоимость доставки СДЭК
      operationId: calculateDeliveryCdek
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ID_функции_calculate-delivery-cdek>
        service_account_id: <ID_сервисного_аккаунта>
      responses:
        '200':
          description: Successful response

  /api/delivery/cdek/points:
    get:
      summary: Получить список пунктов выдачи СДЭК
      operationId: getDeliveryPointsCdek
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ID_функции_get-pvz-cdek>
        service_account_id: <ID_сервисного_аккаунта>
      responses:
        '200':
          description: Successful response

  /api/delivery/cdek/order:
    post:
      summary: Создать заказ в СДЭК
      operationId: createCdekOrder
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ID_функции_create-cdek-order>
        service_account_id: <ID_сервисного_аккаунта>
      responses:
        '200':
          description: Successful response

  /api/delivery/cdek/track:
    get:
      summary: Отследить заказ СДЭК
      operationId: trackCdekOrder
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: <ID_функции_track-cdek-order>
        service_account_id: <ID_сервисного_аккаунта>
      responses:
        '200':
          description: Successful response
```

### 4.2 Обновите API Gateway

1. Перейдите в **API Gateway** в Yandex Cloud Console
2. Выберите ваш API Gateway
3. Нажмите **"Редактировать"**
4. Обновите спецификацию (вставьте новые маршруты)
5. Нажмите **"Сохранить"**

---

## 🔄 Переход с самозанятого на ИП

Когда вы перейдете на ИП:

1. **Получите новые API ключи** в Личном кабинете СДЭК (см. Шаг 1.2)
2. **Обновите переменные окружения** во всех Cloud Functions:
   - `CDEK_CLIENT_ID` → новый Account
   - `CDEK_CLIENT_SECRET` → новый Secure password
3. **Создайте новую версию** каждой функции

**Всё!** Код менять не нужно, он будет работать с новыми ключами.

---

## ✅ Тестирование

### Тестовые ключи СДЭК

Для тестирования используйте:
```bash
CDEK_CLIENT_ID=EMscd6r9JnFiQ3bLoyjJY6eM78JrJceI
CDEK_CLIENT_SECRET=PjLZkKBHEiLK3YsjtNrt3TGNG0ahs3kG
CDEK_TEST_MODE=true
```

### Пример запроса расчета доставки

```bash
curl -X POST https://ваш-api-gateway.apigw.yandexcloud.net/api/delivery/cdek/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "to_location": {
      "code": 270
    },
    "packages": [
      {
        "weight": 1000,
        "height": 10,
        "width": 10,
        "length": 10
      }
    ]
  }'
```

---

## 📚 Полезные ссылки

- **Документация СДЭК API**: https://api-docs.cdek.ru/29923741.html
- **Личный кабинет СДЭК**: https://lk.cdek.ru
- **Заключить договор**: https://cdek.business/
- **Тестовая среда API**: https://api.edu.cdek.ru/v2

---

## ❓ Часто задаваемые вопросы

**Q: Чем отличаются ключи самозанятого и ИП?**  
A: Ничем! API ключи работают одинаково. Разница только в комиссии (5% для самозанятых, 3% для ИП).

**Q: Нужно ли менять код при переходе на ИП?**  
A: Нет, только поменяйте переменные окружения `CDEK_CLIENT_ID` и `CDEK_CLIENT_SECRET`.

**Q: Как тестировать без реального договора?**  
A: Используйте тестовые ключи и установите `CDEK_TEST_MODE=true`.

**Q: Сколько стоит интеграция?**  
A: Интеграция СДЭК полностью бесплатна. Вы платите только за доставку по тарифам СДЭК.
