# 📋 Пошаговая инструкция по запуску интеграции СДЭК

## ✅ Что уже готово (сделано мной)

Я создал для вас:

1. ✅ **Библиотеку для работы с СДЭК API** (`lib/cdek-client.js`)
   - OAuth 2.0 аутентификация
   - Методы для расчета доставки, получения ПВЗ, создания заказов
   - Автоматическое обновление токенов

2. ✅ **4 Cloud Functions**:
   - `calculate-delivery-cdek` - расчет стоимости доставки
   - `get-pvz-cdek` - получение списка пунктов выдачи
   - `create-cdek-order` - создание заказа в СДЭК
   - `track-cdek-order` - отслеживание заказа

3. ✅ **Примеры React компонентов** для фронтенда
4. ✅ **Документацию** по настройке и использованию

---

## 🎯 Что нужно сделать ВАМ

Я буду вас сопровождать на каждом шаге. Делайте по одному шагу и говорите мне когда закончите.

---

## 📝 ШАГ 1: Получите API ключи СДЭК

### Что нужно сделать:

1. Перейдите на сайт **СДЭК для бизнеса**: https://cdek.business/

2. Нажмите **"Заключить договор онлайн"**

3. Выберите ваш статус:
   - **"Самозанятый"** - если вы самозанятый
   - **"ИП"** - если у вас ИП
   - **"ООО"** - если юридическое лицо

4. Заполните анкету:
   - Для самозанятых: понадобится справка из приложения "Мой налог"
   - Для ИП: укажите реквизиты ИП

5. Дождитесь подтверждения договора (обычно 1-2 дня)

6. После заключения договора:
   - Войдите в **Личный кабинет СДЭК**: https://lk.cdek.ru
   - Перейдите в раздел **"Интеграция"**
   - Нажмите **"Создать новый ключ"**

7. **Сохраните** полученные ключи:
   ```
   Account: ваш_логин_здесь
   Secure password: ваш_пароль_здесь
   ```

⚠️ **ВАЖНО:** Если хотите сначала протестировать, можете использовать **тестовые ключи**:
```
Account: EMscd6r9JnFiQ3bLoyjJY6eM78JrJceI
Secure password: PjLZkKBHEiLK3YsjtNrt3TGNG0ahs3kG
```

### ✅ Когда закончите этот шаг, напишите мне: "Шаг 1 готов"

---

## 🔧 ШАГ 2: Загрузите функции в Yandex Cloud

### Что нужно сделать:

1. **Создайте ZIP-архивы** для функций.

   Откройте терминал в корне проекта и выполните:

   ```bash
   cd yandex-functions
   
   # Создаем архивы (включая общую библиотеку lib/)
   zip -r calculate-delivery-cdek.zip calculate-delivery-cdek/ lib/
   zip -r get-pvz-cdek.zip get-pvz-cdek/ lib/
   zip -r create-cdek-order.zip create-cdek-order/ lib/
   zip -r track-cdek-order.zip track-cdek-order/ lib/
   ```

2. **Загрузите каждую функцию в Yandex Cloud**:

   Для каждого архива повторите эти действия:

   a. Откройте **Yandex Cloud Console**: https://console.cloud.yandex.ru
   
   b. Выберите ваш каталог (folder)
   
   c. Перейдите в **Cloud Functions**
   
   d. Нажмите **"Создать функцию"**
   
   e. Введите имя:
      - `calculate-delivery-cdek`
      - `get-pvz-cdek`
      - `create-cdek-order`
      - `track-cdek-order`
   
   f. Нажмите **"Создать"**
   
   g. В редакторе:
      - Выберите **"ZIP-архив"**
      - Загрузите соответствующий zip-файл
      - Точка входа: `index.handler`
      - Среда выполнения: `nodejs18` или `nodejs20`
      - Таймаут: `10` секунд
      - Память: `128 МБ`
   
   h. Добавьте **переменные окружения**:
      ```
      CDEK_CLIENT_ID = ваш_account_из_шага_1
      CDEK_CLIENT_SECRET = ваш_secure_password_из_шага_1
      CDEK_TEST_MODE = false
      ```
      
      (Если используете тестовые ключи, установите `CDEK_TEST_MODE = true`)
   
   i. Нажмите **"Создать версию"**
   
   j. **ВАЖНО:** Скопируйте и сохраните **ID функции** (он выглядит как `d4e...abc123`)
      - Эти ID понадобятся на следующем шаге!

3. **Сделайте функции публичными** (для тестирования):
   
   Для каждой функции:
   - Перейдите на вкладку **"Обзор"**
   - Нажмите **"Права доступа"**
   - Добавьте **"allUsers"** с ролью **"functions.functionInvoker"**

### ✅ Когда закончите, напишите: "Шаг 2 готов. ID функций:"
И скопируйте сюда ID всех 4 функций.

---

## 🌐 ШАГ 3: Обновите API Gateway

### Что нужно сделать:

1. Откройте ваш файл спецификации API Gateway (вероятно `api-gateway-spec.yaml` или один из похожих файлов в корне проекта)

2. Добавьте в раздел `paths:` следующие маршруты:

   ```yaml
   /api/delivery/cdek/calculate:
     post:
       summary: Рассчитать стоимость доставки СДЭК
       operationId: calculateDeliveryCdek
       x-yc-apigateway-integration:
         type: cloud_functions
         function_id: <ID_функции_calculate-delivery-cdek_из_шага_2>
         service_account_id: <ваш_service_account_id>
       responses:
         '200':
           description: OK

   /api/delivery/cdek/points:
     get:
       summary: Получить пункты выдачи СДЭК
       operationId: getDeliveryPointsCdek
       x-yc-apigateway-integration:
         type: cloud_functions
         function_id: <ID_функции_get-pvz-cdek_из_шага_2>
         service_account_id: <ваш_service_account_id>
       responses:
         '200':
           description: OK

   /api/delivery/cdek/order:
     post:
       summary: Создать заказ в СДЭК
       operationId: createCdekOrder
       x-yc-apigateway-integration:
         type: cloud_functions
         function_id: <ID_функции_create-cdek-order_из_шага_2>
         service_account_id: <ваш_service_account_id>
       responses:
         '200':
           description: OK

   /api/delivery/cdek/track:
     get:
       summary: Отследить заказ СДЭК
       operationId: trackCdekOrder
       x-yc-apigateway-integration:
         type: cloud_functions
         function_id: <ID_функции_track-cdek-order_из_шага_2>
         service_account_id: <ваш_service_account_id>
       responses:
         '200':
           description: OK
   ```

3. Замените:
   - `<ID_функции_...>` на реальные ID из Шага 2
   - `<ваш_service_account_id>` на ID вашего сервисного аккаунта

4. Сохраните файл

5. Обновите API Gateway в Yandex Cloud:
   - Откройте **API Gateway** в консоли
   - Выберите ваш gateway
   - Нажмите **"Редактировать"**
   - Скопируйте обновленную спецификацию
   - Нажмите **"Сохранить"**

### ✅ Когда закончите, напишите: "Шаг 3 готов"

---

## 💾 ШАГ 4: Обновите схему базы данных YDB

### Что нужно сделать:

1. Откройте **YDB Console**: https://console.cloud.yandex.ru
2. Перейдите в вашу базу данных
3. Откройте **"Выполнить запрос"**
4. Выполните следующие команды **по одной**:

```sql
ALTER TABLE orders ADD COLUMN deliveryService String;
ALTER TABLE orders ADD COLUMN deliveryType String;
ALTER TABLE orders ADD COLUMN deliveryStatus String;
ALTER TABLE orders ADD COLUMN deliveryAddress String;
ALTER TABLE orders ADD COLUMN deliveryCity String;
ALTER TABLE orders ADD COLUMN deliveryPostalCode String;
ALTER TABLE orders ADD COLUMN deliveryRecipientName String;
ALTER TABLE orders ADD COLUMN deliveryRecipientPhone String;
ALTER TABLE orders ADD COLUMN deliveryPointCode String;
ALTER TABLE orders ADD COLUMN deliveryPointName String;
ALTER TABLE orders ADD COLUMN deliveryPointAddress String;
ALTER TABLE orders ADD COLUMN cdekOrderUuid String;
ALTER TABLE orders ADD COLUMN cdekOrderNumber String;
ALTER TABLE orders ADD COLUMN cdekTrackNumber String;
ALTER TABLE orders ADD COLUMN cdekTariffCode Int64;
ALTER TABLE orders ADD COLUMN cdekDeliveryCost Double;
ALTER TABLE orders ADD COLUMN estimatedDeliveryDays Int64;
ALTER TABLE orders ADD COLUMN deliveryCalculatedAt String;
```

5. Убедитесь, что все команды выполнены успешно

### ✅ Когда закончите, напишите: "Шаг 4 готов"

---

## 🧪 ШАГ 5: Протестируйте интеграцию

### Что нужно сделать:

1. **Получите URL вашего API Gateway** из Yandex Cloud Console

2. **Протестируйте расчет доставки**:

   Откройте терминал и выполните:

   ```bash
   curl -X POST https://ваш-api-gateway.apigw.yandexcloud.net/api/delivery/cdek/calculate \
     -H "Content-Type: application/json" \
     -d '{
       "to_location": {
         "code": 270
       },
       "packages": [
         {
           "weight": 1000,
           "height": 10,
           "width": 10,
           "length": 10
         }
       ]
     }'
   ```

3. **Протестируйте получение ПВЗ**:

   ```bash
   curl "https://ваш-api-gateway.apigw.yandexcloud.net/api/delivery/cdek/points?city_code=270"
   ```

4. Проверьте, что получили ответы без ошибок

### ✅ Когда получите успешные ответы, напишите: "Шаг 5 готов. Все работает!"

---

## 🎨 ШАГ 6: Добавьте компоненты на фронтенд (опционально)

Я создал для вас примеры React компонентов в файле:
`yandex-functions/CDEK_FRONTEND_EXAMPLES.md`

Можете использовать их как основу для создания UI выбора доставки.

### ✅ Этот шаг можете сделать позже, когда будете готовы

---

## 🔄 ПЕРЕХОД С САМОЗАНЯТОГО НА ИП

Когда будете переходить на ИП:

1. Получите новые API ключи в Личном кабинете СДЭК
2. Обновите переменные окружения во всех 4 Cloud Functions:
   - `CDEK_CLIENT_ID` → новый Account
   - `CDEK_CLIENT_SECRET` → новый Secure password
3. Создайте новую версию каждой функции

**Код менять не нужно!** 🎉

---

## ❓ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

Пишите мне на каком шаге возникла проблема и какую ошибку вы получили.
Я помогу разобраться!

---

## 📚 Полезные документы

Все созданные документы находятся в папке `yandex-functions/`:
- `CDEK_SETUP_GUIDE.md` - подробное руководство
- `CDEK_YDB_SCHEMA_UPDATE.md` - обновление схемы БД
- `CDEK_FRONTEND_EXAMPLES.md` - примеры компонентов
- `ПОШАГОВАЯ_ИНСТРУКЦИЯ.md` - этот файл

---

**Готовы начать? Приступайте к Шагу 1! 🚀**
